# Copiá este archivo a .env solo para desarrollo local con `netlify dev`.
# NUNCA subas el .env real a GitHub ni lo pongas en el código del frontend.
# La key real se configura en Netlify: Site settings > Environment variables.
ANTHROPIC_API_KEY=sk-ant-api03-...
